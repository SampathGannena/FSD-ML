# Lightweight production requirements for Render (CPU-only, faster builds)
# Keeps content-based + collaborative recommendations enabled.

numpy>=1.26.0,<2.0.0
scipy>=1.11.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0

pymongo>=4.6.0,<5.0.0
python-dotenv>=1.0.0,<2.0.0
pandas>=2.1.0,<3.0.0
joblib>=1.3.0,<2.0.0

# Intentionally excluded on Render to reduce build time and memory:
# torch
# torch-geometric
# redis
